<% content_for :title, l(:finished_iterations) %>
<% content_for :h2, l(:finished_iterations) %>
<% content_for :actions do %>
  <li><%= link_to l(:active), project_iterations_path %></li>
  <li><%= link_to l(:planned), planned_project_iterations_path %></li>
<% end %>

<% if @iterations.empty? -%>
  <p>
    <%= l(:page_only_finished_iterations) %>
  </p>
  <p>
    <%= l(:no_finished_iterations) %>
  </p>
<% else -%>
  <div id="iterations" class="section_container">
    <div class="section">
      <h2><%= l(:finished) %></h2>
      <div class="finished">
        <% width = @iterations.size == 1 ? 600 : 300 -%>
        <% height = @iterations.size == 1 ? 450 : 225 -%>
        <ul>
          <%= render @iterations, :width => width %>
        </ul>
      </div>
    </div>
  </div>
<% end -%>
