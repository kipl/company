<% content_for :title, l(:planned_iterations) %>
<% content_for :h2, l(:planned_iterations) %>
<% content_for :actions do %>
  <li><%= link_to l(:active), project_iterations_path %></li>
  <li><%= link_to l(:finished), finished_project_iterations_path %></li>
<% end %>

<% if @iterations.empty? -%>
  <p>
    <%= l(:page_only_planned_iterations) %>
  </p>
  <p>
    <%= l(:no_planned_iterations) %>
  </p>
<% else -%>
  <div id="iterations" class="section_container">
    <div class="section">
      <h2><%= %></h2>
      <div class="planned">
        <% width = @iterations.size == 1 ? 600 : 300 -%>
        <% height = @iterations.size == 1 ? 450 : 225 -%>
        <ul>
          <%= render @iterations, :width => width %>
        </ul>
      </div>
    </div>
  </div>
<% end -%>
